build:
  docker:
    web: Dockerfile
run:
  # web: nginx -g "daemon off;"
  # web: /bin/sh -c "envsubst < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
  # web: /bin/sh -c "envsubst '$$PORT' < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
  web: envsubst "$$PORT" < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -g "daemon off;"
  # web: /bin/sh -c "envsubst '$$PORT' < /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
